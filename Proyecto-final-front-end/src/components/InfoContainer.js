import React, { Component } from 'react';

class InfoContainer extends Component {

  render() {
    return (
      <div>
        {'CONTENT HERE (<InfoContainer "props para cada uno"/>)'}
      </div>
    );
  }
}

export default InfoContainer;

//<InfoContainer vale={number} text={texto} />
